l=0, r=n-1, mid = any_valid_idx
while(l <= r) {
    // 1. CHECK : [required condtion] found ? 
    // 2. PARTITION : decide which half to go next?
    // 3. UPDATE : midfor next iteration
}
return mid;