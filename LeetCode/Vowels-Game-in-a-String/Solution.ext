class Solution {
public:
bool isVowel(char c)
{
    if(c=='a' || c=='e' || c=='i' || c=='o' || c=='u') return true;
    return false;
}

    bool doesAliceWin(string s) {
        int count=0;
        for(auto i:s)
            if(isVowel(i)) count++;
        if(count==0) return false;
        else return true;

    }
};